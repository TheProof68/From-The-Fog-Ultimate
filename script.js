document.addEventListener('DOMContentLoaded', () => {
    // Ajoute des fonctionnalités JavaScript si nécessaire
    // Par exemple, ajouter un effet de son lorsque l'utilisateur survole les sections
    const buttons = document.querySelectorAll('.button');
    buttons.forEach(button => {
        button.addEventListener('mouseover', () => {
            // Jouer un son effrayant ici si nécessaire
        });
    });
});